# mlx-rs

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-03 02:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-rs is a Rust library that provides bindings and high-level abstractions over Apple's MLX machine learning framework. The repository is organized into multiple crates: a core library exposing MLX array operations and neural network primitives to Rust, procedural macro utilities that support code generation and builder patterns (reducing boilerplate for neural network module definitions), and example applications demonstrating practical usage including language model inference (Mistral, Qwen3) and neural network training (MNIST MLP). The core library wraps MLX's C/C++ API through FFI, enabling Rust developers to construct and execute ML computations on Apple hardware (leveraging Metal/GPU acceleration where available). The procedural macros assist in deriving traits and generating builder structs for neural network layers and models. Dependencies flow inward: examples depend on the core library and macros; the core library depends on the MLX native framework via FFI bindings. No other Rust crates appear to depend on mlx-rs within this repository, though it is intended for consumption by external Rust ML projects targeting Apple platforms.

## Projects

### mlx-rs

mlx-rs is a Rust library providing bindings and high-level abstractions over Apple's MLX machine learning framework. The repository includes the core library, procedural macro utilities for code generation and builder patterns, and example applications demonstrating language model inference (Mistral, Qwen3) and neural network training (MNIST MLP). The project enables Rust developers to build and run machine learning models using MLX's array operations and neural network primitives.

