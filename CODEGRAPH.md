# mlx-rs

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-03 22:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-rs is a Rust binding library for Apple's MLX machine learning framework. It exposes MLX's tensor operations, neural network layers (via a high-level nn module), and training utilities through idiomatic Rust APIs. The repository is organized into several crates: a core library implementing MLX operations (array creation, arithmetic, linear algebra, FFT, convolutions, etc.), a procedural macro crate (mlx-internal-macros) that generates boilerplate code for module parameters and other repetitive patterns, and example applications demonstrating practical usage including language model inference (Mistral, Qwen3) and MNIST digit classification. The core library uses Rust FFI to call into the underlying MLX C++ library, wrapping the C API bindings to provide memory-safe, ergonomic Rust interfaces. The crates within the repository are tightly coupled: examples depend on the core library and nn module, and the core library depends on mlx-internal-macros for code generation. Externally, the entire stack depends on the MLX framework being available as a native library (primarily targeting Apple Silicon via Metal GPU acceleration). No downstream dependents are identified within this repository.

## Projects

### mlx-rs

mlx-rs is a Rust binding/wrapper library for the MLX machine learning framework, providing tensor operations, neural network layers, and training utilities. The repository includes example applications (language models like Mistral and Qwen3, MNIST digit classification), a macro crate (mlx-internal-macros) for code generation, and the core library implementing MLX operations in Rust.

