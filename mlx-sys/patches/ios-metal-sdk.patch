diff --git a/CMakeLists.txt b/CMakeLists.txt
index f8ee65ad..9b69a51b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -220,6 +220,25 @@ if(MLX_BUILD_METAL)
       OUTPUT_VARIABLE MLX_METAL_VERSION
       OUTPUT_STRIP_TRAILING_WHITESPACE COMMAND_ERROR_IS_FATAL ANY)
     message(STATUS "Metal version: ${MLX_METAL_VERSION}")
+  elseif(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+    # pmetal: cross-compiling for iOS. The macOS probe above cannot run (sw_vers
+    # reports the host, and `xcrun -sdk macosx` is the wrong SDK), so select the
+    # iOS toolchain explicitly and probe the Metal version the same way.
+    set(MLX_METAL_SDK "iphoneos")
+    if(NOT CMAKE_OSX_DEPLOYMENT_TARGET STREQUAL "")
+      set(MLX_METAL_VERSION_MIN_FLAG
+          "-mios-version-min=${CMAKE_OSX_DEPLOYMENT_TARGET}")
+    endif()
+    execute_process(
+      COMMAND
+        zsh "-c"
+        "echo \"__METAL_VERSION__\" | xcrun -sdk ${MLX_METAL_SDK} metal ${MLX_METAL_VERSION_MIN_FLAG} -E -x metal -P - | tail -1"
+      OUTPUT_VARIABLE MLX_METAL_VERSION
+      OUTPUT_STRIP_TRAILING_WHITESPACE COMMAND_ERROR_IS_FATAL ANY)
+    message(
+      STATUS
+        "Building for iOS ${CMAKE_OSX_DEPLOYMENT_TARGET}, Metal version: ${MLX_METAL_VERSION}"
+    )
   else()
     set(MLX_METAL_VERSION 0)
   endif()
diff --git a/mlx/backend/metal/kernels/CMakeLists.txt b/mlx/backend/metal/kernels/CMakeLists.txt
index edc169ee..431702b9 100644
--- a/mlx/backend/metal/kernels/CMakeLists.txt
+++ b/mlx/backend/metal/kernels/CMakeLists.txt
@@ -9,6 +9,17 @@ set(BASE_HEADERS
     logging.h
     utils.h)
 
+# pmetal: the Metal SDK and the version-min flag are platform-specific. Upstream hardcodes
+# `-sdk macosx` / `-mmacosx-version-min`, which silently produces macOS kernels when
+# cross-compiling to iOS: the build succeeds and the metallib fails on device.
+if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
+  set(MLX_METAL_SDK "iphoneos")
+  set(MLX_METAL_VERSION_MIN_PREFIX "-mios-version-min=")
+else()
+  set(MLX_METAL_SDK "macosx")
+  set(MLX_METAL_VERSION_MIN_PREFIX "-mmacosx-version-min=")
+endif()
+
 function(build_kernel_base TARGET SRCFILE DEPS)
   set(METAL_FLAGS
       -x
@@ -25,11 +36,12 @@ function(build_kernel_base TARGET SRCFILE DEPS)
     set(METAL_FLAGS ${METAL_FLAGS} -fmetal-enable-logging)
   endif()
   if(NOT CMAKE_OSX_DEPLOYMENT_TARGET STREQUAL "")
-    set(METAL_FLAGS ${METAL_FLAGS}
-                    "-mmacosx-version-min=${CMAKE_OSX_DEPLOYMENT_TARGET}")
+    set(METAL_FLAGS
+        ${METAL_FLAGS}
+        "${MLX_METAL_VERSION_MIN_PREFIX}${CMAKE_OSX_DEPLOYMENT_TARGET}")
   endif()
   add_custom_command(
-    COMMAND xcrun -sdk macosx metal ${METAL_FLAGS} -c ${SRCFILE}
+    COMMAND xcrun -sdk ${MLX_METAL_SDK} metal ${METAL_FLAGS} -c ${SRCFILE}
             -I${PROJECT_SOURCE_DIR} -o ${TARGET}.air
     DEPENDS ${SRCFILE} ${DEPS} ${BASE_HEADERS}
     OUTPUT ${TARGET}.air
@@ -180,12 +192,13 @@ endif()
 
 set(METAL_LINK_FLAGS)
 if(NOT CMAKE_OSX_DEPLOYMENT_TARGET STREQUAL "")
-  set(METAL_LINK_FLAGS "-mmacosx-version-min=${CMAKE_OSX_DEPLOYMENT_TARGET}")
+  set(METAL_LINK_FLAGS
+      "${MLX_METAL_VERSION_MIN_PREFIX}${CMAKE_OSX_DEPLOYMENT_TARGET}")
 endif()
 
 add_custom_command(
   OUTPUT ${MLX_METAL_PATH}/mlx.metallib
-  COMMAND xcrun -sdk macosx metal ${METAL_LINK_FLAGS} ${KERNEL_AIR} -o
+  COMMAND xcrun -sdk ${MLX_METAL_SDK} metal ${METAL_LINK_FLAGS} ${KERNEL_AIR} -o
           ${MLX_METAL_PATH}/mlx.metallib
   DEPENDS ${KERNEL_AIR}
   COMMENT "Building mlx.metallib"
